 <div class="container">
   <h1>Editing Participant</h1>

   <%= render 'form', participant: @participant %>

   <% if current_user.admin? %>
     <%= link_to 'Destroy', @participant, method: :delete, data: { confirm: 'Are you sure?' } %> |
   <% end %>

   <%= link_to 'Show', @participant %> |
   <%= link_to 'Back', participants_path %>
 </div>
